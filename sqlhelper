Requirements :
"Please Select" Dropdown with Include/Exclude Fields: You want to add a dropdown where users can choose between "Include" or "Exclude", and this will dictate where the selected programs go when clicked "Add".
Select/Deselect All Programs: If the "Select All" checkbox is deselected, all the program checkboxes should be deselected. If it’s selected, all checkboxes should be selected.
Add Selected Programs to Include/Exclude Boxes: After selecting programs and choosing "Include" or "Exclude" via the dropdown, when the user clicks "Add", the selected program(s) should be moved to the appropriate "Include" or "Exclude" list box based on the dropdown choice.
dont make any small change just add the change accordingly and dont remove the three text at the last of the ui
